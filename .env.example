# Recommended: call your own backend/serverless endpoint so the OpenAI API key is not exposed in the browser.
VITE_OPENAI_PARSER_ENDPOINT=

# Browser-direct fallback for local testing only. Do not expose this in a public deploy.
VITE_OPENAI_API_KEY=
VITE_OPENAI_MODEL=gpt-5-mini
